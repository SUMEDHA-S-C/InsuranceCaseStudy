@model IEnumerable<Insurance.Models.tbl_policyType>

@{
    ViewBag.Title = "ShowDetails";
}

<h2>ShowDetails</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PolicyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PolicyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PlanNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.PlanNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PlanNumber })
        </td>
    </tr>
}

</table>
